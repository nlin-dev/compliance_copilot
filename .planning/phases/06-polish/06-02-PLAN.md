---
phase: 06-polish
plan: 02
type: execute
---

<objective>
Create comprehensive README documentation and Vercel deployment configuration.

Purpose: Enable others to set up and deploy the project, document architecture and usage.
Output: Complete README.md and vercel.json configuration file.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./06-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior work
@.planning/phases/06-polish/06-01-SUMMARY.md

# Key files
@package.json
@.env.example
@next.config.mjs
@prisma/schema.prisma

**Tech stack:** Next.js 14, OpenAI, Pinecone, Upstash Redis, Prisma/Postgres
**Scripts:** npm run dev, npm run build, npm run ingest, npm test
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive README</name>
  <files>README.md</files>
  <action>
    Replace the placeholder README with comprehensive documentation:

    ## Structure:
    1. **Title + Description** - CMS Compliance Assistant, brief value proposition
    2. **Features** - Bullet list: Q&A chat, compliance checker, source citations
    3. **Tech Stack** - List: Next.js 14, OpenAI, Pinecone, Upstash, Prisma, shadcn/ui
    4. **Prerequisites** - Node.js 18+, required API accounts (OpenAI, Pinecone, Upstash, Postgres)
    5. **Getting Started**
       - Clone repo
       - Install deps: `npm install`
       - Copy `.env.example` to `.env` and fill in values
       - Run Prisma: `npx prisma generate && npx prisma db push`
       - Run ingestion: `npm run ingest` (downloads and processes CMS manual)
       - Start dev: `npm run dev`
    6. **Environment Variables** - Table with each var, description, where to get it
    7. **Scripts** - Table: dev, build, start, test, ingest
    8. **Architecture** - Brief overview: offline ingestion → vector DB → RAG retrieval → LLM response
    9. **API Endpoints** - POST /api/query, POST /api/compliance with brief descriptions
    10. **Deployment** - Link to Vercel, note about env vars
    11. **License** - MIT

    Keep it practical and focused on getting started. No unnecessary badges or fluff.
  </action>
  <verify>README.md is readable, all code blocks are valid, links work</verify>
  <done>README contains all sections, provides clear setup path from clone to running</done>
</task>

<task type="auto">
  <name>Task 2: Create Vercel deployment configuration</name>
  <files>vercel.json</files>
  <action>
    Create vercel.json with production-ready configuration:

    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "framework": "nextjs",
      "regions": ["iad1"],
      "functions": {
        "src/app/api/**/*.ts": {
          "maxDuration": 10
        }
      },
      "crons": []
    }
    ```

    Configuration choices:
    - `regions: ["iad1"]` - US East for low latency to OpenAI/Pinecone (both US-based)
    - `maxDuration: 10` - Match Vercel Hobby tier limit, explicit for clarity
    - No rewrites/redirects needed - Next.js App Router handles routing
    - No crons - ingestion is manual via npm script

    Do NOT add headers here - they're already in next.config.mjs.
  </action>
  <verify>vercel.json is valid JSON, `npx vercel build` succeeds locally if Vercel CLI installed</verify>
  <done>vercel.json exists with appropriate configuration for Hobby tier deployment</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README.md contains all 11 sections
- [ ] vercel.json is valid JSON
- [ ] `npm run build` still succeeds
- [ ] No sensitive values in committed files
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- README enables new developer setup
- Vercel config ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-02-SUMMARY.md`
</output>
